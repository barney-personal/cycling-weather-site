// Reserved for ambient declarations. d3 is now imported as ESM sub-packages
// (see `src/lib/data.ts`, `src/main.ts`, `src/history.ts`); no globals to
// declare. File kept so future ambient additions land in a stable location.
export {};
